import { Story, Meta } from "@storybook/react"
import {Check as Component} from "../buttons"

export default {
    title: 'Components/Buttons/Check',
    component: Component,
} as Meta

export const Check: Story = () => <Component />
